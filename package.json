{
  "name": "redtone-static-cards",
  "version": "1.0.0",
  "description": "Static HTML card generator from Supabase",
  "type": "module",
  "scripts": {
    "generate": "node generate.js",
    "build": "cd admin && npm install && npm run build",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}
